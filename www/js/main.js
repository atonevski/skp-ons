// Generated by CoffeeScript 2.3.2
var CENTER, map, renderMap, showAlert, showToast;

showAlert = function(m) {
  return ons.notification.alert(m);
};

showToast = function(m) {
  var t;
  t = 2000;
  if (arguments.length > 1) {
    t = arguments[1];
  }
  return ons.notification.toast(m, {
    timeout: t
  });
};

map = null;

CENTER = [41.99249998, 21.42361109];

renderMap = function() {
  map = L.map('map-id').setView(CENTER, 12);
  // L.tileLayer 'http://{s}.tile.osm.org/{z}/{x}/{y}.png', {}
  L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {}).addTo(this.map);
  return console.log(`base64: ${atob('abc')}`);
};

renderMap();
